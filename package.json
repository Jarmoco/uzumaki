{
  "name": "usumaki",
  "module": "index.ts",
  "type": "module",
  "workspaces": [
    "crates/usumaki_core",
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "start": "pnpm --filter playground start",
    "build:core": "pnpm --filter usumaki build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
